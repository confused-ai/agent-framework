# Human In The Loop

This mirrored guide now points to the primary HITL documentation.

- Use the main guide for approvals, review flows, and stores.
- Keep approval logic on the current public API instead of legacy package layouts.
- Start with a minimal approval gate and add routing only when required.