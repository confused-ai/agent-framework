# Hooks

This mirrored guide now points to the primary hooks documentation.

- Use `createAgent({ hooks })` and the main guide for the current lifecycle shape.
- The checked examples live in the primary docs tree.
- Keep mirrored content prose-only so it does not drift from the runtime contracts.