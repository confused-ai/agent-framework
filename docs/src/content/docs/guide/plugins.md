# Plugins

This mirrored guide now points to the primary plugins guide.

- Use the main docs tree for the current plugin extension story.
- Keep imports on the public package surface rather than internal package names.
- Prefer source-backed primary examples for implementation details.