/**
 * Production module: runtime, control plane, resilience.
 *
 * @deprecated Import directly from `@confused-ai/production` instead.
 *   This re-export facade will be removed in the next major version.
 */

export * from '@confused-ai/production';
